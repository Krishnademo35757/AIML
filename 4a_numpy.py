#pip install numpy

import numpy as np

# Example usage
arr = np.array([1, 2, 3, 4, 5])
print(arr)
print(np.mean(arr))     # Mean of array
print(np.sum(arr))      # Sum of array
print(np.max(arr))      # Maximum value in array
